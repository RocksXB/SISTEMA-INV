import { useEffect,useMemo,useState } from 'react'; import { subscribeInventory } from '../services/inventoryService'; import type { HydratedInventoryItem,InventoryItem,ItemDefinition } from '../types';
export function useInventory(characterId:string|undefined,items:ItemDefinition[]){const [raw,setRaw]=useState<InventoryItem[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{if(!characterId){setLoading(false);return}return subscribeInventory(characterId,v=>{setRaw(v);setLoading(false)},()=>{setError('Falha ao sincronizar o inventário.');setLoading(false)})},[characterId]);const inventory=useMemo(()=>raw.map(entry=>{const definition=items.find(x=>x.id===entry.itemId);return definition?{...entry,definition}:null}).filter((x):x is HydratedInventoryItem=>x!==null),[raw,items]);return {raw,inventory,loading,error}}
