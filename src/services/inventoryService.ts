import { collection,deleteDoc,doc,getDocs,onSnapshot,query,runTransaction,serverTimestamp,updateDoc,where,type Unsubscribe } from 'firebase/firestore'; import { db } from '../lib/firebase'; import type { EquipmentSlot, InventoryItem, ItemDefinition } from '../types'; import { canEquipItem } from '../utils/inventory';
const inv=(cid:string)=>collection(db,'characters',cid,'inventory');
export const subscribeInventory=(cid:string,cb:(v:InventoryItem[])=>void,error:(e:Error)=>void):Unsubscribe=>onSnapshot(inv(cid),s=>cb(s.docs.map(d=>({id:d.id,...d.data()}) as InventoryItem)),error);
export async function equipItem(cid:string,entry:InventoryItem,item:ItemDefinition,slot:EquipmentSlot,all:InventoryItem[]){const check=canEquipItem(item,slot,all,entry.id);if(!check.allowed)throw new Error(check.reason);await updateDoc(doc(inv(cid),entry.id),{equipped:true,equipmentSlot:slot,updatedAt:serverTimestamp()})}
export const unequipItem=(cid:string,id:string)=>updateDoc(doc(inv(cid),id),{equipped:false,equipmentSlot:null,updatedAt:serverTimestamp()});
export async function deliverItem(cid:string,item:ItemDefinition,amount:number){const qty=Math.floor(amount);if(qty<1)throw new Error('Quantidade inválida.');if(!item.stackable&&qty!==1)throw new Error('Itens não empilháveis devem ser entregues individualmente.');if(item.stackable&&qty>item.maxStack)throw new Error(`Máximo permitido: ${item.maxStack}.`);const existing=await getDocs(query(inv(cid),where('itemId','==',item.id)));await runTransaction(db,async tx=>{if(item.stackable&&!existing.empty){const target=existing.docs[0],current=target.data().quantity as number,next=current+qty;if(next>item.maxStack)throw new Error(`A pilha excederia o máximo de ${item.maxStack}.`);tx.update(target.ref,{quantity:next,updatedAt:serverTimestamp()})}else{const target=doc(inv(cid));tx.set(target,{itemId:item.id,quantity:qty,equipped:false,equipmentSlot:null,acquiredAt:serverTimestamp(),updatedAt:serverTimestamp()})}})}
export async function setQuantity(cid:string,entry:InventoryItem,item:ItemDefinition,qty:number){qty=Math.floor(qty);if(qty<=0)return deleteDoc(doc(inv(cid),entry.id));if(!item.stackable&&qty!==1)throw new Error('Item não empilhável só aceita quantidade 1.');if(qty>item.maxStack)throw new Error(`Máximo permitido: ${item.maxStack}.`);return updateDoc(doc(inv(cid),entry.id),{quantity:qty,updatedAt:serverTimestamp()})}
export const removeInventoryItem=(cid:string,id:string)=>deleteDoc(doc(inv(cid),id));
